{
    "module": {
        "autoreply": {
            "enable": false,
            "prefixed": true,
            "delay": 1,
            "file": "doc/local.csv",
            "msg": {
                "help": [
                    "When message is sent to the bot\n",
                    "the bot pickup one information from the CSV file and provide it after a delay of 2s. The CSV file content can be managed via the **CSV** module.",
                    "- **help**: this help\n",
                    "_Example_: `autoreply help`"
                ]
            }
        }
    }
}
